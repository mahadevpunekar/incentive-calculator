# Backend

Reserved for the future **NestJS** service (REST/GraphQL, auth, incentive engine, Postgres).

The **frontend** workspace is the active app. Run the UI from the repository root:

```bash
npm install
npm run dev
```
