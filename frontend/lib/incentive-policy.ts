/**
 * Mock plan rules — replace with config from policy engine / HR.
 * Used by payout curve zones and copy.
 */
export const PAYOUT_THRESHOLD_ACHIEVEMENT_PCT = 80;
export const TARGET_BAND_MAX_ACHIEVEMENT_PCT = 100;
